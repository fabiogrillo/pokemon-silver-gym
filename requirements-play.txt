# Minimal runtime deps for the Dockerized playback demo (agents/rl/play.py).
# Inference + visualization only — NO GPU. torch (CPU build) is installed separately in the
# Dockerfile from the PyTorch CPU wheel index, so it is intentionally NOT listed here.
pyboy==2.7.1
stable-baselines3==2.8.0
gymnasium==1.2.3
numpy==2.4.4
pillow==12.2.0
imageio==2.37.3
PySDL2==0.9.17
pysdl2-dll==2.32.0
cloudpickle==3.1.2
tqdm==4.67.3
